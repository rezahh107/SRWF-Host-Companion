name: Apply approved product vision amendment

on:
  push:
    paths:
      - '.github/workflows/apply-product-vision.yml'

permissions:
  contents: write

jobs:
  amend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke
        run: echo "workflow-ready"
